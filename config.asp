<%

' @global string Password A string (encapsulated by quotes).

Password = "example"

' The directory where files should be uploaded to.
'
' This directory path may be absolute or relative to this script file and should not end
' with a \. It should point to an existing directory on the server, where IIS service can write.
'
' @global string MediaRoot A string (encapsulated by quotes).

MediaRoot = "photos"

%>